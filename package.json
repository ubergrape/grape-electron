{
  "devDependencies": {
    "asar": "^0.10.0",
    "electron-prebuilt": "0.37.8",
    "fs-jetpack": "^0.7.0",
    "gulp": "^3.9.0",
    "gulp-less": "^3.0.3",
    "gulp-util": "^3.0.6",
    "q": "^1.4.1",
    "rollup": "^0.25.4",
    "tree-kill": "^0.1.1",
    "yargs": "^4.2.0",
    "node-uuid": "^1.4.7"
  },
  "optionalDependencies": {
    "appdmg": "^0.3.2",
    "rcedit": "^0.4.0"
  },
  "scripts": {
    "postinstall": "cd app && npm install",
    "build": "gulp build",
    "release:exe": "gulp release --env=production",
    "release:msi": "gulp release --env=production --wix",
    "release:exe:staging": "gulp release --env=staging",
    "release:msi:staging": "gulp release --env=staging --wix",
    "start": "node ./tasks/start",
    "test": "node ./tasks/start --env=test",
    "install-native": "node ./tasks/install_native_module"
  },
  "dependencies": {
    "electron-osx-sign": "^0.3.0"
  }
}
